# 記事メディア 1,000本 進捗と再開手順

`ARTICLE_SPEC.md` と `rules/*.md` を読んでから作業してください。
サイト全体の禁止事項は `CLAUDE.md` が優先します。

## 再開するとき

```
git pull
node check-articles.js       # out/ に書きかけが無いか確認
```

1. このファイルの作業ログを読み、どこまで終わっているか確認する
2. `data/article_plan.tsv` から**まだ書いていない行**を引く（`media/{slug}.html` が無いもの）
3. `ARTICLE_SPEC.md` の記事タイプ別の構成に従って `out/{slug}.md` を書く
4. **条番号を書くときは `node tools/egov.js <法令ID> <条番号>` で原文を確認する**
5. **出典URLは実際に開いて到達を確認してから `sources` に書く**
6. `node check-articles.js` … 警告ゼロにする
7. `node build-articles.js` … `media/*.html`・`media/index.html`・`sitemap.xml` を更新
8. ローカルで表示を確認する（`http://localhost:8788/media/`）
9. このファイルの作業ログに1行足す
10. **内容を松岡さんに提示して承認を得てから push する**

## 決めごと

- **1セッションで書くのは3〜5本。** それ以上は品質が落ちる
- **見解ブロックが2箇所ない記事は公開しない**
- 残業時間・年収額を訴求に使わない。出典付きの統計を本文で解説するのは可
- 特定の自治体の内部事情（残業・人間関係・部署の評判）は書かない。裏が取れない
- 2端末で並行するときは、**種別で担当を分ける**か、着手前に必ず `git pull` する

## 内訳（`data/article_plan.tsv`・全1,000本）

| No | 種別 | 本数 |
|---|---|---|
| 1-350 | 自治体別 | 350（都道府県47／政令市20／特別区23／中核市62／施行時特例市23／市175） |
| 351-550 | 部署別 | 200（40部署 × 5テーマ） |
| 551-700 | 選考対策 | 150 |
| 701-850 | 業界研究 | 150（30業界 × 5テーマ） |
| 851-950 | 制度とお金 | 100 |
| 951-1000 | 資格・スキル | 50 |

## 作業ログ

最終更新：2026-09-10（**自動運転4回目。北関東3県。**No.8 `pref-ibaraki`（5,034字）／No.9 `pref-tochigi`（5,091字）／No.10 `pref-gunma`（5,029字）。`check-articles.js` は out/ 全15本 **OK15／NG0**、タグ一致・JSON-LD正常・リンク切れなし・異種文字なし。
**この回で見つけた論点：地域手当の「国の指定基準」と「実際の支給割合」がずれている県がある。**茨城は国基準6.0%に対し実支給約7.46%、栃木は3.5%に対し約4.40%、**群馬は2.5%で基準どおり**。総務省Excelの列にこの2つが並んでいるので、以降の県でも必ず両方を見て、差があれば記事の論点にする。**地域手当は民間にない概念**なので、手当の名目ごとに求人票と比べると必ず噛み合わない、という話につなげられる。
**もう一点：茨城の平均年齢41.8歳はここまでで最も若い。**平均年齢は「平均給料月額が高く出る要因」でもあるので、ラスパイレス指数（補正済み）と混ぜて読まないよう毎回書き分ける。
**北関東は「転居せずに首都圏の求人に応募できる」**という前提が東北と決定的に違う。**勤務地から検索すると都内本社・リモート可の求人が視界に入らない**ので、職種で絞ってから勤務形態を確認する順序を勧める形にした。
⚠ 初稿を厚く書く方針にしたら **4,600〜4,950字**まで伸び、往復が1〜2回に減った。「技術職の探し方」「在職中の注意点」「面接で詰められること」「残る判断」の4節を最初から入れるのが効く。
**残り985本**（うち需要ランクS 225本）。次は **No.11 埼玉県以降**）


最終更新：2026-09-10（**自動運転3回目。都道府県3本。**No.5 `pref-akita`（5,010字）／No.6 `pref-yamagata`（5,069字）／No.7 `pref-fukushima`（5,036字）。`check-articles.js` は out/ 全12本 **OK12／NG0**、タグ一致・JSON-LD正常・リンク切れなし・異種文字なし。**これで東北6県＋北海道が揃った。**
**総務省Excel（`000984154.xlsx`）だけで書けるようになり、調査の手間が大きく減った。**1団体1行を `node tools/xlsx.js <ファイル> 1 <団体名>` で引くだけ。
**この回で書けた論点：**（1）**秋田は地域手当ゼロでラスパイレス100.1**＝給料表そのものが国と同水準。一方で諸手当61,662円は近隣で最も低い部類。（2）**山形は平均年齢43.7歳で東北最高**。平均給料月額が高いのはこの年齢構成の影響も含むので、**ラスパイレス指数（補正済み）と平均給料月額（補正なし）を混ぜて読まない**。（3）**福島は地域手当ゼロで諸手当81,193円**（東北2位）。地域手当が入っていない以上それ以外の手当の積み上がりであり、**業務が変われば額も変わり得る**。複数年の源泉徴収票でぶれ幅を見るべき、という書き方にした。
⚠ **自治体別は下限5,000字が重く、初稿では毎回4,300字前後で止まる。**節を足す往復が発生するので、次からは「技術職の探し方」「在職中の注意点」「面接で詰められること」など**再利用できる節を最初から入れて書く**。
**残り988本**（うち需要ランクS 228本）。次は **No.8 茨城県以降**）


最終更新：2026-09-10（**自動運転2回目。都道府県3本＋道具1つ。**No.2 `pref-aomori`（5,006字）／No.3 `pref-iwate`（5,021字）／No.4 `pref-miyagi`（5,103字）。`check-articles.js` は out/ 全9本 **OK9／NG0**。
**この回の成果：`tools/xlsx.js` を作った。**総務省「地方公共団体給与情報等公表システム」の集約Excelを **npm依存なしで**読める（zipはzlib、XMLは素直な走査）。これで**自治体別350本の共通ソースが確保できた**。団体別のラスパイレス指数・平均年齢・平均給料月額・諸手当月額・平均給与月額・国ベースが1ファイルから引ける（`https://www.soumu.go.jp/main_content/000984154.xlsx`）。
⚠ **列の対応は自分で検証すること。**結合セルのせいでヘッダーがずれており、**手当の内訳（000984159）は列と項目の対応が確定できなかったので使わなかった**。使ったのは北海道の公表PDFと突き合わせて位置を確認できた項目だけ（平均年齢42.5・平均給料月額318,800がPDFの42.5歳・318,816円と一致）。
⚠ **`<rPh>` はふりがな。**先に落とさないと「都道府県名トドウフケンメイ」になる。`tools/xlsx.js` で対処済み。
⚠ **ハングルなど異種文字の混入が3回起きた**（`성립` など。見た目が似ていて気づけない）。`check-articles.js` に検出を追加し、frontmatterを含めて弾くようにした。
**この回で書けた論点：**（1）青森県はラスパイレス96.8・地域手当なしで東北でも低いほう、岩手県は99.4・地域手当なしで**給料表そのものが国に近い**、宮城県は100.0で**諸手当92,195円と近隣より2万円以上高い**（地域手当あり）。（2）**地域手当は民間にない概念**なので、手当の名目ごとに民間の提示額と比べると必ず噛み合わない。源泉徴収票の支払金額で比べる。
**残り991本**（うち需要ランクS 231本）。次は **No.5 秋田県以降**。データは総務省Excelから引ける）


最終更新：2026-09-10（**自動運転1回目。需要ランクSから3本。**No.1 `pref-hokkaido`（5,052字）／No.855 `seido-taishokuteate-fushime`（4,074字）／No.894 `seido-shitsugyo-daikae`（4,033字）。`check-articles.js` は **out/ 全6本 OK6／NG0**、タグ一致・JSON-LD正常・記事内リンク切れなし。
**この回で分かったこと：**（1）**自治体の「給与・定員管理等の状況」は各自治体サイトで様式も置き場所もバラバラ。**北海道は `tools/pdftext.js` でPDFから職員数61,623人・部門別内訳・退職手当支給率まで取れたが、**青森県は標準様式の公表ページに到達できず、この回は飛ばした**（CLAUDE.md 4-3 の「届かない自治体は飛ばす」に従った）。（2）**総務省「地方公共団体給与情報等公表システム」に全団体分の集約Excelがある**（`https://www.soumu.go.jp/main_sosiki/jichi_gyousei/c-gyousei/teiin-kyuuyo02_R06.html`）。ラスパイレス指数・平均給与月額・退職手当の支給状況などが団体別に入っている。**自治体別350本はこれを共通ソースにするべき。**次回、xlsxを読む道具を作ってから自治体別に戻る。（3）北海道の「退職手当1人当たり平均支給額2,352千円」は**全職種の退職者の平均**で、定年まで勤めた場合の額ではない。この種の数字は注記を読まずに使うと誤る。
**残り994本**（うち需要ランクS 234本）。次は**総務省Excelを読む道具を作り、都道府県 No.2以降**へ）


最終更新：2026-09-10（**記事メディアの土台を作り、1本目を書きました。**`data/article_plan.tsv` を1,000本ぶん生成（slug重複0・ローマ字欠落0）。`ARTICLE_SPEC.md`／`rules/common.md`・`cta.md`・`insight.md`／`build-plan.js`／`build-articles.js`／`check-articles.js`／`tools/egov.js` を新規作成。ヘッダーnav（`template.html`・`apply-template.html`・`zukan.html`）と `zukan.html` のフッターに「記事」を追加し、`node rebuild.js` を実行（**生成物の差分は各3行のみ・求人119件の並びは不変**）。
**この回で分かったこと：**（1）**e-Gov の HTML ページは JavaScript で本文を描画するため、取得しても条文が読めない。**法令APIから引く `tools/egov.js` を作って解決した。地方自治法211条・233条・149条を原文で確認済み。（2）**Bash のヒアドキュメント経由で JS を書くとバックスラッシュが1段消え、文字列から組み立てた正規表現が静かに壊れる。**正規表現を含むファイルはエディタ側で書くこと。
（3）**e-Gov の法令IDは推測しない。**国家公務員退職手当法を `328AC0000000182` と推測して404。正しくは **`328AC1000000182`**（AC0ではなくAC1）。`node tools/egov.js --find <法令名>` で必ず引くこと。
**書いた記事は3本。**`check-articles.js` は **OK3／NG0**、HTMLのタグ不一致なし、構造化データ（Article・BreadcrumbList・FAQPage）も正常。
- **No.351 `dept-zaisei-honyaku`**（財政課の経験は民間で何と呼ばれるか）5,021字。根拠＝地方自治法211条・233条・149条
- **No.851 `seido-taishokuteate-shikumi`**（退職手当の仕組み）4,031字。根拠＝地方自治法204条・204条の2、国家公務員退職手当法3条・6条の4。**支給率は21〜25年の100分の200が山で、26年以降は下がる**という条文の形を明示した
- **No.893 `seido-shitsugyo-nashi`**（公務員に雇用保険がない理由）4,035字。根拠＝雇用保険法6条6号、国家公務員退職手当法10条（失業者の退職手当）。**「公務員だから除外」ではなく「退職手当等が求職者給付を超えると認められるから除外」**という構造を条文で示した

**残り997本。**次は No.352 以降（部署別の続き）か、需要ランクSの自治体別 No.1-90 から）
